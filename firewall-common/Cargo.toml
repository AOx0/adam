[package]
name = "firewall-common"
version = "0.1.0"
edition = "2021"

[features]
default = []
user = ["aya"]
bpf = ["aya-ebpf"]

[dependencies]
aya = { version = "0.12", optional = true }
aya-ebpf = { version = "0.1.0", optional = true }
netp.workspace = true

[lib]
path = "src/lib.rs"
